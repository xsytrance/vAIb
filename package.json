{
  "name": "vaib",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4013",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4013",
    "api:library": "node server/api.mjs",
    "sync:agents": "node scripts/sync-agent-roster.mjs",
    "sync:cockpit": "node scripts/sync-cockpit-bridge.mjs",
    "sync:all": "npm run sync:agents && npm run sync:cockpit"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.4"
  }
}
